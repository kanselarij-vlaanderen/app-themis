{
  "version": "0.1",
  "scripts": [
    {
      "documentation": {
        "command": "generate-mandatees",
        "description": "Generate migrations to reflect changes within government. Optionally accepts a path to the TTL dump file of the latest government dataset. When omitted, the latest dump is looked up in the triplestore.",
        "arguments": ["path-to-dataset-file"]
      },
      "environment": {
        "image": "kanselarij/generate-mandatees-script",
        "interactive": true,
        "join_networks": true,
        "script": "./generate-mandatees/main.py"
      },
      "mounts": {
        "app": "/data/app/"
      }
    },
    {
      "documentation": {
        "command": "generate-dataset",
        "description": "Generate a DCAT distribution, dataset and file of the minister data. This script is typically run after all changes for a new government/minister hav been applied. It creates a snapshot of the current government.",
        "arguments": []
      },
      "environment": {
        "image": "kanselarij/generate-dataset-script",
        "interactive": false,
        "join_networks": true,
        "script": "./generate-dataset/main.py"
      },
      "mounts": {
        "app": "/data/app/"
      }
    }
  ]
}
