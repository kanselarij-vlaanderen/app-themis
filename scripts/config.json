{
  "version": "0.1",
  "scripts": [
    {
      "documentation": {
        "command": "generate-mandatees",
        "description": "Generate migrations to reflect changes within government",
        "arguments": ["dataset-dump (optional, defaults to the latest dump according to Virtuoso)"]
      },
      "environment": {
        "image": "kanselarij/generate-mandatees-script",
        "interactive": true,
        "join_networks": true,
        "script": "./generate-mandatees/main.py"
      },
      "mounts": {
        "app": "/data/app/"
      }
    },
    {
      "documentation": {
        "command": "generate-dataset",
        "description": "Generate dcat distribution, dataset and file",
        "arguments": []
      },
      "environment": {
        "image": "kanselarij/generate-dataset-script",
        "interactive": false,
        "join_networks": true,
        "script": "./generate-dataset/main.py"
      },
      "mounts": {
        "app": "/data/app/"
      }
    }
  ]
}
